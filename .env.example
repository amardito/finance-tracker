# Build-time API URL for Vite (frontend talks to the API at this origin).
# In dev: leave blank to use Vite's same-origin proxy or set explicitly.
# In docker-compose: also exported as VITE_API_URL so the build picks it up.
VITE_API_URL=http://localhost:4000
