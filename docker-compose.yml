services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Point this at your running API (defaults assume finance-tracker-api on localhost:4000)
        VITE_API_URL: ${VITE_API_URL:-http://localhost:4000}
    restart: unless-stopped
    ports:
      - '8080:80'
